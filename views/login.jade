extends layout

block content
  form(action="", method="post" class='w-50 mx-auto text-white fs-5 mt-5 pt-5')
    h1(class='text-info text-center mb-4') Login to you account
    div(class='mb-4 d-flex flex-column w-50 mx-auto')
      label(class='mb-2') Username 
      input(type='text')
    div(class='mb-4 d-flex flex-column w-50 mx-auto')
      label(class='mb-2') Password 
      input(type='password')
      button(type='submit' class='btn text-white bg-info my-5 w-50 mx-auto') Login